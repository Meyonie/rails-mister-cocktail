

    <h1 class="show-title"> <%= @cocktail.name.capitalize %> </h1>
<ul>
  <% @cocktail.doses.each do |dose| %>
    <li><%= dose.ingredient.name %> <%= link_to 'Delete', dose_path(dose), method: :delete %></li>
  <% end %>
</ul>

<%= link_to 'Mets la dose', new_cocktail_dose_path(@cocktail) %>
<%= link_to 'Retour', cocktails_path %>
